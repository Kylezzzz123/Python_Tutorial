coordinates = (4, 5) # () means you can't change or edit or remove the elements
# coordinates[1] = 10                 #  error message, you can't change it
print( coordinates[0])

num = [(1, 2), (2,5)]
print(num)